<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:background="@drawable/gradient_bg">

    <LinearLayout
        android:id="@+id/tv_day_date"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:orientation="vertical"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottom_divider">

    <!-- 1. Date Information (Left Side) -->
    <TextView
        android:id="@+id/tv_day_of_week"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Mon"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@color/text_dark_primary" />
    <TextView
        android:id="@+id/tv_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Nov 18"
        android:layout_marginVertical="4dp"
        android:textSize="14sp"
        android:textColor="@color/text_secondary_gray"
        />


    </LinearLayout>



    <LinearLayout
        android:id="@+id/layout_steps_water_calories"
        android:layout_width="0dp"
        android:layout_height="60dp"
        android:orientation="horizontal"
        android:weightSum="3"
        android:layout_marginStart="24dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottom_divider"
        app:layout_constraintStart_toEndOf="@id/tv_day_date"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Metric 1: Steps (Using user's ic_flame choice for steps icon) -->
        <LinearLayout
            android:id="@+id/layout_steps"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center_horizontal">

            <com.google.android.material.imageview.ShapeableImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_steps"
                app:tint="@color/text_dark_primary"
                tools:ignore="ContentDescription" />

            <TextView
                android:id="@+id/tv_steps_summary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="10,000"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="@color/text_dark_primary"/>
        </LinearLayout>

        <!-- Metric 2: Water -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center_horizontal">

            <com.google.android.material.imageview.ShapeableImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_drop"
                android:layout_gravity="center"
                app:tint="@color/accent_water_blue"
                tools:ignore="ContentDescription" />

            <TextView
                android:id="@+id/tv_water_summary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="2,500 ml"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="@color/accent_water_blue"/>
        </LinearLayout>

        <!-- Metric 3: Calories -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:gravity="center_horizontal">

            <com.google.android.material.imageview.ShapeableImageView
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:src="@drawable/ic_flame"
                app:tint="@color/accent_calories_orange"
                android:layout_gravity="center"
                tools:ignore="ContentDescription" />

            <TextView
                android:id="@+id/tv_calories_summary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                tools:text="450 kcal"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="@color/accent_calories_orange"/>
        </LinearLayout>

    </LinearLayout>

    <!-- 3. Full-Width Bottom Divider for List Separation -->
    <View
        android:id="@+id/bottom_divider"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="@color/bg_extra_light_gray"
        android:layout_marginTop="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginStart="-16dp"
        android:layout_marginEnd="-16dp"/> <!-- Negative margins stretch it to the edges -->

</androidx.constraintlayout.widget.ConstraintLayout>